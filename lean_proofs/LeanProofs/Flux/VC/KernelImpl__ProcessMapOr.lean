import LeanProofs.Flux.Prelude
open Classical

namespace F



def KernelImpl__ProcessMapOr := 
 ∀ (c0 : Prop),
  ∀ (default₀ : Int),
   ∀ (closure₀ : Int),
    ∀ (a'₂ : Prop),
     (a'₂ = True) ->
      False ->
       (c0)
end F
